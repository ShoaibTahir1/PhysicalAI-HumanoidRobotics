# Contracts Directory

This project is a static documentation site with no backend API, so there are no API contracts to define. The site consists entirely of static content served through Docusaurus.